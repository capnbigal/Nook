@page "/archive"
<PageTitle>Archive · Nook</PageTitle>

<NodeListView Title="Archive" Icon="@Icons.Material.Filled.Archive"
              Description="Archived nodes are hidden from daily views but keep all their history, connections and actions. Restore any of them from its card."
              EmptyText="No archived nodes." Filter="_filter" ShowDates="true" />

@code {
    private readonly NodeFilter _filter = new() { ArchivedOnly = true };
}
